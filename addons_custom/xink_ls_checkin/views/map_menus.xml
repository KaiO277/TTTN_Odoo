<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Client Action: Mở bản đồ -->
    <record id="action_map_demo_sale" model="ir.actions.client">
        <field name="tag">xink_ls_checkin.MapDemoClientAction</field>
        <field name="target">current</field>
        <field name="context">{ "module": "sale" }</field>
    </record>

    <!-- Menu: Gắn vào module Sale -->
    <menuitem id="menu_map_demo_sale"
              name="Bản đồ check-in"
              parent="sale.sale_menu_root"
              sequence="120"
              action="action_map_demo_sale"/>
</odoo>
