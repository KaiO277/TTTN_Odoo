<odoo>
    <data noupdate="0">
        <!-- 1. Ẩn menu cha "To Invoice" -->
        <record id="sale.menu_sale_invoicing" model="ir.ui.menu">
            <field name="active">False</field>
        </record>
        <!-- 2. Ẩn khách hàng ở Đơn hàng -->
        <record id="sale.res_partner_menu" model="ir.ui.menu">
            <field name="active">False</field>
        </record>

        <!-- 3. Di chuyển "Invoice" sang menu "Orders" -->
        <menuitem id="sale.menu_sale_order_invoice"
                  name="Orders To Invoice"
                  parent="sale.sale_order_menu"
                  sequence="60"/>

        <!-- 4. Thêm Bộ phận bán hàng vào menu "Orders" -->
        <menuitem id="sale.report_sales_team" name="Sales Teams" parent="sale.menu_sale_report" sequence="60"/>
    </data>
</odoo>
