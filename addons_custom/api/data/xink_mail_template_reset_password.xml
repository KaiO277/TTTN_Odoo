<odoo>
    <record id="xink_mail_template_reset_password" model="mail.template">
        <field name="name">XINK - Reset Password OTP</field>
        <field name="model_id" ref="base.model_res_users"/>
        <field name="subject">Your OTP for password reset</field>
        <field name="email_from">${email_from}</field>
        <field name="email_to">${object.partner_id.email}</field>
        <field name="auto_delete" eval="False"/>
        <field name="body_html">
            <![CDATA[
            <p>Hello ${object.name},</p>
            <p>Your OTP for password reset is: <strong>${otp}</strong></p>
            <p>This OTP will expire at: ${format_datetime(expiration, tz=object.company_id.tz or 'UTC', dt_format='dd/MM/yyyy HH:mm')}</p>
            <p>Trân trọng,<br>${object.company_id.name}</p>
            <hr>
            <p>
                Đây là email tự động, vui lòng không trả lời trực tiếp. Nếu cần hỗ trợ, liên hệ qua ${object.company_id.email}.
            </p>
            ]]>
        </field>
    </record>
</odoo>
