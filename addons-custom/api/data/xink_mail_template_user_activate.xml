<odoo>
    <record id="xink_mail_template_user_activate" model="mail.template">
        <field name="name">XINK - Activate Account by Email</field>
        <field name="model_id" ref="base.model_res_users"/>
        <field name="subject">Kích hoạt tài khoản của bạn trên ${object.company_id.name}</field>
        <field name="email_from">${email_from}</field>
        <field name="email_to">${object.partner_id.email}</field>
        <field name="auto_delete" eval="False"/>
        <field name="body_html"><![CDATA[
            <div>
                <h2>Xin chào ${object.name},</h2>
                <p>Cảm ơn bạn đã đăng ký tại ${object.company_id.name}.</p>
                <p>Vui lòng nhấp vào liên kết bên dưới để kích hoạt tài khoản của bạn:</p>
                <p>
                    <a href="${api_url}/api/user/activate?token=${object.partner_id.signup_token}">
                        Kích hoạt tài khoản
                    </a>
                </p>
                <p>Liên kết này sẽ hết hạn vào:
                    ${format_datetime(object.partner_id.signup_expiration, tz=object.company_id.tz or 'UTC', dt_format='dd/MM/yyyy HH:mm')}
                </p>
                <p>Trân trọng,<br>${object.company_id.name}</p>
                <hr>
                <p>
                    Đây là email tự động, vui lòng không trả lời trực tiếp. Nếu cần hỗ trợ, liên hệ qua ${object.company_id.email}.
                </p>
            </div>
        ]]></field>
    </record>
</odoo>